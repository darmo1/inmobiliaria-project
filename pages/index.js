import Head from 'next/head'
import Image from 'next/image'
import React from 'react'
import ContainerService from '../components/ContainerService'
import styles from '../styles/Home.module.css'
import {ProductsList} from '../Utils/ProductsList'

export default function Home() {
  return (
    <React.Fragment>
      <Head>
        <title>Inmobiliaria Don Quijote</title>
        <meta name="description" content="Generated by Inmobiliaria Don Quijote" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <ContainerService>
        {
          ProductsList.map( product => {
            return (
              <div key={product.id}>
                  <h2>{product.serviceName}</h2>
                  <h4>{product.extract}</h4>
              </div>
            )
          })
        }
      </ContainerService>



      
    </React.Fragment>
  )
}
