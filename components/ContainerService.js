import React from 'react'

const ContainerService = ({children}) => {
    return (
        <section>
            <ul>
                {children}
            </ul>
        </section>
    )
}

export default ContainerService
