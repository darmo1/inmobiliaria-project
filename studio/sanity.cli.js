import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId:  "68f3de3x", // replace value with your own
    dataset: "production" // replace value with your own
  }
})